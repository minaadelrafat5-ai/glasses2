import { PagePlaceholder } from '@/components/shared';

export function CartPage() {
  return (
    <PagePlaceholder
      badge="Cart"
      title="Your cart"
      description="The cart page will show items you've added and let you check out. Cart state is already wired up and ready to power it."
    />
  );
}
