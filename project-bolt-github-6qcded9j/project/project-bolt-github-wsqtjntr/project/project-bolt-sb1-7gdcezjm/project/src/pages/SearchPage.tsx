import { PagePlaceholder } from '@/components/shared';

export function SearchPage() {
  return (
    <PagePlaceholder
      badge="Search"
      title="Search"
      description="Search across the catalog by frame, brand, or style. Arriving with the catalog in the next phase."
    />
  );
}
