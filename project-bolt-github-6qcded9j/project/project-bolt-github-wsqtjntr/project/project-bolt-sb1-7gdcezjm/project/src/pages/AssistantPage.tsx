import { PagePlaceholder } from '@/components/shared';

export function AssistantPage() {
  return (
    <PagePlaceholder
      badge="AI"
      title="Recommendation Assistant"
      description="Describe your style, budget, and face shape, and the assistant will match you to frames. The recommendation service contract is defined and ready to connect."
    />
  );
}
